"""Legacy components kept for reference.

`legacy.planner` is the earlier text-only LLM planner (Anthropic / OpenAI APIs).
It is superseded by `planning/` (VLM planner via Ollama), which the demos use.
"""
